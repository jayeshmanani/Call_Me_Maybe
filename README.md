First, run the following commands to set up the required environment and install all dependencies needed to run the project successfully.


```bash
# Display the setup repos required for the goinfre setup
make uname

# Generate the .env.goinfre environment file
make env

# Load the environment variables into the current terminal session
source .env.goinfre

# Install all project dependencies
make install
```

