[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Config file for UV for call me maybe"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=2.1.0",
    "numpy>=2.2.6",
    "pydantic>=2.13.4",
]
